{% extends 'base.html.twig' %}

{% block title %}Films{% endblock %}

{% block body %}

    <div class="w-75">

        <h2>Liste des films {#<a class="d-inline" href="{{ path('game_add') }}"><i class="material-icons">add_circle</i></a>#}</h2>
        <div class="col-12 d-flex flex-row flex-wrap justify-content-around">
            {% for movie in movies %}
                <div class="card col-5 col-lg-2 mb-3 me-lg-1">
                    <div class="card-header p-0">
                        <a href="{{ path('movie_details', {'id': movie.id}) }}">
                            <img class="w-100" src="/image/movie/poster/{{ movie.slug }}.jpeg">
                        </a>
                    </div>
                    <div class="card-body">
                        <p>
                            {{ movie.name }}
                        </p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
